{
  "name": "leads-crm-webhook-listener",
  "version": "1.0.0",
  "description": "Webhook listener para notificações de eventos de leads do PostgreSQL",
  "main": "webhook_listener.js",
  "scripts": {
    "start": "node webhook_listener.js",
    "dev": "nodemon webhook_listener.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.6.2",
    "dotenv": "^16.3.1",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18"
  }
}
