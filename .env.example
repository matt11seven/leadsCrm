# Configurações do PostgreSQL
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=leadscrm
POSTGRES_PORT=5432

# Configuração do Webhook
WEBHOOK_URL=http://localhost:8080/api/lead-event

# Variáveis para o deploy (apenas para referência, não são usadas localmente)
# TARGET_HOST=seu-servidor.com
# TARGET_USER=seu-usuario
# TARGET_DIR=/caminho/para/deploy
